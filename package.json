{
  "name": "promptvault-chrome",
  "version": "1.1.0",
  "private": true,
  "description": "Dependency-free verification for the PromptVault browser extension.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint": "node --check src/core.js && node --check src/app.js && node scripts/verify-extension.cjs",
    "verify": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
